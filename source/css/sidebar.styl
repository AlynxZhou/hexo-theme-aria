@import "variables";
@import "nib";
.sidebar {
  background: $color-sidebar-background;
  flex-grow: 1;
  flex-shrink: 0;
  flex-basis: 25%
  width: 25%;
  padding: 0;
  word-wrap: break-word;
  // word-break: break-all;
  .search {
    height: $search-height - $search-padding-top;
    display: block;
    text-align: center;
    background: $color-search-background;
    color: $color-search;
    padding-top: $search-padding-top;
    input {
      width: 60%;
      height: 1.5em;
      border: 0.1em solid transparent;
      border-bottom: 0.1em dashed $color-search;
      background: $color-search-background;
      color: $color-search;
      transition: 0.3s ease-in-out;
      &:focus {
        outline: none;
        border-bottom: 0.1em solid $color-search;
      }
      &::placeholder {
        color: $color-search;
        padding-left: 0.5em;
        opacity: 0.7;
      }
    }
  }
  .search-result-box {
    background: white;
    word-warp: break-word;
    transition: 0.3s ease-in-out;
    .search-result-list {
      margin-top: 0;
      padding: 1em 2em;
    }
    &:hover {
      box-shadow: 0 0.075em 1em 0.5em rgba(0, 0, 0, 0.19);
    }
  }
  .sidebar-item {
    margin: 2em;
  }
  .rss {
    display: block;
    text-align: center;
    font-size: 1.5em;
    color: $color-link-index-button;
    background: $orange;
    margin: 1em;
    padding: 0.2em;
    line-height: 1.25em;
  }
  hr {
    margin: 2em 0;
    height: 0.3em;
    border: none;
    margin-left:auto;
    margin-right:auto;
    width: 100%;
    position: relative;
    display: block;
    box-sizing: content-box;
    background-color: #ddd;
    background-image: repeating-linear-gradient(-45deg, #fff, #fff 0.25em, transparent 0.25em, transparent 0.5em);
  }
  @media (max-width: 48em) {
    width: 100%;
    .social-link, .blogroll {
      text-align: center;
      ul {
        padding: 0;
        li {
          display: inline-block;
          margin: 0.5em 1em;
        }
      }
    }
    .post-toc {
      text-align: center;
      ol {
        li {
          text-align: left;
        }
      }
    }
  }
}
